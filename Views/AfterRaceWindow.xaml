<Window x:Class="AMS2ChEd.Views.AfterRaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Race Results" Height="640" Width="900" WindowStartupLocation="CenterOwner" Background="{StaticResource WindowBackground}">
    <Grid Margin="12">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel>
            <TextBlock Text="Race Results" FontSize="20" FontWeight="Bold" DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Content="Save and Return" Click="SaveReturn_Click"/>
                <Button Content="Next Race" Click="NextRace_Click"/>
            </StackPanel>
        </DockPanel>


        <Grid Grid.Row="1" Margin="0,12,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <ListView x:Name="ResultsList" Margin="0,0,12,0"/>


            <Border Background="{StaticResource CardBackground}" CornerRadius="8" Padding="10">
                <StackPanel>
                    <TextBlock Text="Championship Updated" FontWeight="SemiBold"/>
                    <ListView x:Name="UpdatedStandings"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
