<Window x:Class="AMS2ChEd.Views.LetterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AMS2ChEd.Views"
        xmlns:vm="clr-namespace:AMS2ChEd.ViewModels"
        mc:Ignorable="d"
        Title="LetterView" Height="450" Width="800" >

    <Grid>
        <Border Background="#FFFDF7E3"
                BorderBrush="SaddleBrown"
                BorderThickness="3"
                CornerRadius="12"
                Margin="20">

            <StackPanel Margin="30,30,30,30">

                <TextBlock Text="{Binding LetterHeader}" FontSize="24" FontWeight="Bold" FontStyle="Italic" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock Text="{Binding LetterBody}" TextWrapping="Wrap" FontSize="20" FontFamily="Segoe Script" Margin="0,0,0,20"/>

                <Button Content="Accept Offer" Command="{Binding AcceptCommand}" Margin="0,0,0,10"/>
                <Button Content="Go Back" Command="{Binding BackCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
